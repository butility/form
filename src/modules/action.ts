
export * from "@/core/action";
import {
    scrollToError,
    showErrorMessage,
    hideErrorMessage,
    enableForm,
    enableInput,
    disableForm,
    disableInput,
    toggleInputValidity,
} from "@/core/action";

const FormAction = {
    scrollToError,
    showErrorMessage,
    hideErrorMessage,
    enableForm,
    enableInput,
    disableForm,
    disableInput,
    toggleInputValidity,
}

export default FormAction;